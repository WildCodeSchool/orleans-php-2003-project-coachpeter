{% extends 'base_admin.html.twig' %}

{% block title %}InfoCoach{% endblock %}

{% block body %}
    <h1>InfoCoach</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ infoCoach.id }}</td>
            </tr>
            <tr>
                <th>Catchline</th>
                <td>{{ infoCoach.catchline }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ infoCoach.image }}</td>
            </tr>
            <tr>
                <th>Philosophy</th>
                <td>{{ infoCoach.philosophy }}</td>
            </tr>
            <tr>
                <th>Presentation</th>
                <td>{{ infoCoach.presentation }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ infoCoach.phone }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ infoCoach.mail }}</td>
            </tr>
            <tr>
                <th>Adress</th>
                <td>{{ infoCoach.adress }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ infoCoach.city }}</td>
            </tr>
            <tr>
                <th>ZipCode</th>
                <td>{{ infoCoach.zipCode }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('info_coach_index') }}">back to list</a>

    <a href="{{ path('info_coach_edit', {'id': infoCoach.id}) }}">edit</a>

    {{ include('admin_info_coach/_delete_form.html.twig') }}
{% endblock %}
