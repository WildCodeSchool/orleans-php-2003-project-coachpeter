{% extends 'base_admin.html.twig' %}

{% block title %}Modifier une ressource{% endblock %}

{% block body %}
    <h1 class="text-primary text-center">Modifier la ressource "{{ ressource.name }}"</h1>

    {{ include('admin_ressource/_form.html.twig', {'button_label': 'Enregistrer'}) }}

    <div class="w-75 m-auto">
        <a class="btn btn-light text-primary border-primary" href="{{ path('program_index') }}">Retour</a>
    </div>

{% endblock %}
