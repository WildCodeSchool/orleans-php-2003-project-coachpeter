{% extends 'base_admin.html.twig' %}

{% block title %}Nouvelle étape{% endblock %}

{% block body %}
    <h1 class="text-primary text-center">Créer une nouvelle étape</h1>
    <h2 class="text-primary text-center">{{ program.name }}</h2>
    <div class="w-75 m-auto text-center">
    {{ include('admin_program_step/_form.html.twig') }}
    </div>
    <a class="btn border-primary" href="{{ path('program_index') }}">Retour</a>
{% endblock %}

