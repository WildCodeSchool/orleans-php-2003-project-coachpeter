{% extends 'base_admin.html.twig' %}

{% block title %}Voir le programme{% endblock %}

{% block body %}
    <h1 class="text-center text-primary">Voir le programme</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ program.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ program.name }}</td>
            </tr>
            <tr>
                <th>Duration</th>
                <td>{{ program.duration }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('program_index') }}">back to list</a>

    <a href="{{ path('program_edit', {'id': program.id}) }}">edit</a>

    {{ include('program/_delete_form.html.twig') }}
{% endblock %}
