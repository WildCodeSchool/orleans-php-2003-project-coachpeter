{{ form_start(themeForm) }}
<div class="form-group col-4 offset-4 text-primary text-center">
    {{ form_label(themeForm.nameTheme) }}
    {{ form_errors(themeForm.nameTheme) }}
    {{ form_widget(themeForm.nameTheme) }}
</div>
<div class="form-group col-4 offset-4 text-primary text-center">
</div>
<div class="form-inline justify-content-center">
    <button class="btn btn-primary m-2">{{ button_label|default('Enregistrer') }}</button>
</div>
{{ form_end(themeForm) }}
