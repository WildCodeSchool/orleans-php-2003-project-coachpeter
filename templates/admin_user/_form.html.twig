{{ form_start(form) }}
<div class="form-group text-primary m-auto">
<div class="text-center">
    {{ user.firstname }}
    {{ user.lastname }}

    {{ form_widget(form) }}
</div>
</div>

<div class="form-inline justify-content-center">
    <button class="btn btn-primary m-4">{{ button_label|default('Enregistrer') }}</button>
</div>
{{ form_end(form) }}
