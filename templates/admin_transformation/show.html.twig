{% extends 'base_admin.html.twig' %}

{% block title %}Transformation{% endblock %}

{% block body %}
    <h1 class="text-center text-primary">Transformation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ transformation.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ transformation.name }}</td>
            </tr>
            <tr>
                <th>Pound</th>
                <td>{{ transformation.pound }}</td>
            </tr>
            <tr>
                <th>PictureBefore</th>
                <td>{{ transformation.pictureBefore }}</td>
            </tr>
            <tr>
                <th>PictureAfter</th>
                <td>{{ transformation.pictureAfter }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('transformation_index') }}">back to list</a>

    <a class="btn btn-primary" href="{{ path('transformation_edit', {'id': transformation.id}) }}">edit</a>

    {{ include('admin_transformation/_delete_form.html.twig') }}
{% endblock %}
