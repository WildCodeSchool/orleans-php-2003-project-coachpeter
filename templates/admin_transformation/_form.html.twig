{{ form_start(form) }}


<table class="table w-50 m-auto">
    <thead>
    </thead>
    <tbody>
    <tr>
        <th>Nom</th>
        <td>
            {{ form_errors(form.name) }}
            {{ form_widget(form.name) }}
        </td>
    </tr>
    <tr>
        <th>Poids perdu</th>
        <td>
            {{ form_errors(form.pound) }}
            {{ form_widget(form.pound) }}
        </td>
    </tr>
    <tr>
        <th>Photo avant</th>
        <td>
            {{ form_errors(form.pictureBeforeFile) }}
            {{ form_widget(form.pictureBeforeFile) }}
        </td>
    </tr>
    <tr>
        <th>Photo après</th>
        <td>
            {{ form_errors(form.pictureAfterFile) }}
            {{ form_widget(form.pictureAfterFile) }}
        </td>
    </tr>
    </tbody>
</table>

    {{ form_widget(form) }}
<div class="text-center">
    <button class="btn btn-primary m-2">{{ button_label|default('Enregistrer') }}</button>
</div>
{{ form_end(form) }}
