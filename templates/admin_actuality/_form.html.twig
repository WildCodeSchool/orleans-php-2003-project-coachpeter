
{{ form_start(form) }}
<div class="form-inline justify-content-center m-2">
    {{ form_row(form.title) }}
</div>
<div class="form-inline justify-content-center m-2">
    {{ form_row(form.date) }}
</div>
<div class="form-inline justify-content-center m-2">
    {{ form_row(form.theme) }}
</div>
<div class="form-inline justify-content-center m-2">
    {{ form_row(form.actualityFile) }}
</div>
<div class="form-inline justify-content-center m-2">
    {{ form_row(form.content) }}
</div>
    <div class="form-inline justify-content-center">
        <button class="btn btn-primary m-2">{{ button_label|default('Enregistrer') }}</button>
    </div>
{{ form_end(form) }}
