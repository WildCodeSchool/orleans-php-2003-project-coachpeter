{% extends 'base.html.twig' %}

{% block title %}Attended{% endblock %}

{% block body %}
    <h1>Attended</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ attended.id }}</td>
            </tr>
            <tr>
                <th>BeginDate</th>
                <td>{{ attended.beginDate ? attended.beginDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>EndDate</th>
                <td>{{ attended.endDate ? attended.endDate|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('attended_index') }}">back to list</a>

    <a href="{{ path('attended_edit', {'id': attended.id}) }}">edit</a>

    {{ include('attended/_delete_form.html.twig') }}
{% endblock %}
